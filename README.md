Cancel Request Service for Angular
==================================

CancelRequestService allows you to cancel pending $http reqests using the `timeout` propert of the $http config object. Currently cancels all pending requests.
